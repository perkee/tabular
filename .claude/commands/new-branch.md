Create a new feature branch from an up-to-date main.

Steps:
1. `git checkout main`
2. `git pull origin main`
3. `git checkout -b $ARGUMENTS`

The argument should be a kebab-case branch name describing the feature (e.g. `add-dark-mode`).
