{
  "private": true,
  "scripts": {
    "build": "lamdera make src/Frontend.elm --output=/dev/null",
    "test": "elm-test --compiler lamdera",
    "review": "elm-review",
    "check": "npm run build && npm run test && npm run review && lamdera check",
    "test:e2e": "npx playwright test",
    "create-pr": "scripts/create-pr.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "elm": "^0.19.1-6",
    "elm-review": "^2.13.5",
    "elm-test": "^0.19.1-revision12"
  }
}
